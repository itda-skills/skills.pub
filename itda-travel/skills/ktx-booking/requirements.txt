# ktx-booking 의존성 (SPEC-KTX-BOOKING-001)
#
# KTX 예약은 코레일 비공식 모바일 API(letskorail.com)를 사용합니다.
# 동작에는 안티봇(Dynapath) 대응이 된 korail2 계열 라이브러리가 필요하며,
# 구체 패키지·버전은 라이브 동작 검증 시점에 살아있는 것으로 확정합니다(OQ-1).
# 어댑터(korail_adapter.py)는 라이브러리 부재 시 설치 안내로 fail-loud 합니다.
#
# 설치 예: uv pip install --system pycryptodome <korail2 계열 패키지>

pycryptodome>=3.20.0
