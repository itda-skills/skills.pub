# realty-jeonse-gap 스킬 의존성
# Python 표준 라이브러리만 사용 (외부 패키지 없음)
# spec.md R7: stdlib only, 외부 런타임 의존성 없음
#
# 사용 표준 라이브러리:
#   - json (출력 포맷)
#   - argparse (CLI)
#   - urllib (HTTP — deals_collector 경유)
#   - xml.etree.ElementTree (XML 파싱 — deals_collector 경유)
