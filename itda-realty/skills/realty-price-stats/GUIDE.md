---
title: "realty-price-stats 사용 가이드"
---

# realty-price-stats 사용 가이드

한국부동산원 R-ONE 가격지수·전월세전환율과 실거래 데이터 기반 파생 통계(평균·중위·최대·최솟값)를 제공합니다.
**Claude에게 말로 부탁하면 됩니다** — "강남구 아파트 주간 가격지수 6개월치 가져와줘", "분당구 최근 3개월 평균·중위 매매가 통계 보여줘"처럼요. 터미널 명령을 직접 입력할 필요는 없습니다.

---

## 처음 설정하기

### R-ONE 가격지수 조회

한국부동산원 R-ONE Open API 키가 필요합니다.

1. [reb.or.kr](https://www.reb.or.kr/r-one/openapi/)에 회원가입합니다.
2. Open API 활용신청을 합니다: https://www.reb.or.kr/r-one/openapi/SttsApiTblData.do
3. 발급받은 키를 **Claude Desktop 지침**에 추가합니다 — Claude Desktop → **설정 → 일반 → "Claude 지침"** 에 아래 한 줄을 붙여넣고 저장하세요.

```dotenv
RONE_API_KEY=발급받은_키
```

한 번 넣으면 이후 모든 대화에서 Claude가 자동으로 사용합니다 — `.env` 같은 파일을 직접 만들 필요가 없습니다.

> 개발자라면 작업 폴더 루트의 `.env`에 같은 한 줄을 넣어도 됩니다.

### 실거래 파생 통계 추가 조회

실거래 데이터 기반 파생 통계(평균·중위·최대·최솟값)를 함께 보려면 공공데이터포털 키도 필요합니다. `realty-deals` 스킬을 이미 쓰고 있다면 같은 키를 그대로 씁니다. 이 키도 같은 자리(**설정 → 일반 → "Claude 지침"**)에 한 줄 추가하면 됩니다.

```dotenv
KO_DATA_API_KEY=발급받은_키
```

> KB 데이터허브는 공식 API가 없어 스크래핑하지 않습니다. KB 데이터가 필요하면 공식 다운로드 페이지(https://data.kbland.kr)에서 직접 파일을 받아 활용하세요.

---

## 지원하는 지수 유형

| 유형 | 설명 |
|------|------|
| 주간 가격지수 | 아파트 가격의 단기 흐름 추적 |
| 월간 가격지수 | 아파트 가격의 중장기 추세 파악 |
| 전월세전환율 | 전세에서 월세로 전환 시 적용 비율 추이 |

---

## 자주 쓰는 요청

| 하고 싶은 것 | 이렇게 말하세요 |
|--------------|-----------------|
| 주간 가격지수 조회 | "2026년 1~6월 아파트 주간 가격지수 가져와줘" |
| 월간 가격지수 조회 | "2026년 상반기 아파트 월간 가격지수 보여줘" |
| 전월세전환율 추이 | "2026년 1~6월 전월세전환율 추이 조회해줘" |
| 실거래 파생 통계 | "강남구 2026년 1월 아파트 매매 평균·중위·최댓값 통계 보여줘" |
| 단지별 통계 비교 | "강남구 2026년 상반기 아파트 매매를 단지별로 묶어서 통계 보여줘" |

---

## 알아두면 좋은 점

- **두 가지 데이터 출처**: R-ONE 지수(한국부동산원 공식 시계열)와 실거래 파생 통계(공공데이터포털 raw 데이터 기반)는 목적이 다릅니다. 시장 흐름 추적에는 R-ONE 지수가, 특정 지역·기간의 시세 분포 파악에는 파생 통계가 유용합니다.
- **단지별 비교**: 실거래 파생 통계를 단지명 기준으로 묶어달라고 하면 단지별로 평균·중위·최대·최솟값을 비교할 수 있습니다.
- **KB 데이터**: KB 데이터허브는 공식 API를 제공하지 않아 이 스킬에서 직접 조회할 수 없습니다. 필요 시 https://data.kbland.kr에서 직접 파일을 다운로드하세요.

---

## 안 될 때

| 증상 | 원인 / 해결 |
|------|-------------|
| "RONE 키가 없다"는 안내 (`config`) | **Claude 지침**(또는 `.env`)에 `RONE_API_KEY`가 없거나 비어 있음. 위 "처음 설정하기" 확인 |
| "공공데이터 키가 없다"는 안내 (`config`) | 실거래 파생 통계 요청 시 `KO_DATA_API_KEY`도 필요. **Claude 지침**(또는 `.env`)에 추가 |
| API 서비스 오류 (`api`) | 각 포털 활용신청 승인 상태를 확인하고 다시 요청 |
