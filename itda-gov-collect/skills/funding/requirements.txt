# curl_cffi: Safari TLS 지문 위장 HTTP 클라이언트.
# **선택 의존**이다 — 미설치 시 표준 urllib 경로로 동작하며(stderr 에 1회 명시
# 고지) 테스트도 전건 통과한다. 다만 TLS 지문 검사를 하는 소스가 urllib 요청을
# 차단할 수 있으므로 실사용·CI 에서는 설치를 권장한다.
curl_cffi>=0.15
