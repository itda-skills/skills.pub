# brain-fixture 의존성 — 원장에서 오피스 문서를 결정론 렌더하고 독립 재파싱한다.
python-docx>=1.1.0    # docx 렌더 + 재파싱
openpyxl>=3.1.0       # xlsx 렌더 + 재파싱
python-pptx>=1.0      # pptx 렌더 + 재파싱
reportlab>=4.0        # pdf 텍스트 레이어 렌더(한글 CID 폰트 내장)
pypdf>=4.0            # verify.py 의 pdf 독립 재파싱(텍스트 추출)
