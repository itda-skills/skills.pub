# Changelog

이 플러그인의 주요 변경 사항을 기록합니다. 형식은 [Keep a Changelog](https://keepachangelog.com/), 버전은 [SemVer](https://semver.org/)를 따릅니다.

## [0.14.0] — 2026-08-25

앱 데이터가 76항목 → **784항목**(교재 전량 × 3언어)으로 늘면서 검수기가 새 콘텐츠에서
파서 결함 3개를 잡아냈다. 76항목에서는 드러나지 않던 자리들이다.

### Fixed — 병음 음절 분리

- **n·ng 경계가 어긋나던 것.** `lángān`(栏杆)을 최장일치로 자르면 `láng+ān` 이 되지만
  옳은 것은 `lán+gān` 이다. 병음 표기법상 **모음으로 시작하는 음절 앞에는 격음부호(’)가
  필요**하므로, 부호가 없는데 다음이 모음으로 시작하면 그 분리가 틀린 것이다.
  실제로 생성된 한글이 `랑안`·`낭워`·`땅아오`·`컨엉` 으로 나가 있었다.
  (`dàngāo` 蛋糕 · `kěnéng` 可能 · `nánguò` 难过 도 같은 자리)
- **단 `er` 은 성모를 받지 않는다.** `yīngér`(婴儿)에서 g 를 넘기면 `gér` 라는 없는 음절이
  된다 — 그 자리는 원래대로 둔다. (위 규칙을 넣자마자 4건이 이렇게 깨졌다)
- **`respellSentence` 가 격음부호를 잃던 것.** `qǐ’é` 가 `qǐé` 로 나가 다시 읽으면
  `qiě` 가 됐다. 낱말 안에서 모음으로 시작하는 음절 앞에 ’ 를 다시 넣는다.

### Added

- `transcribeZh(pinyin, {hanzi})` — 한자를 주면 C8·C9(不·一)를 **정확히** 적용한다.
  음절과 한자를 짝지어 一·不 인 자리만 고르므로 동음자(衣·布)가 걸리지 않는다.
  0.12.0 에서 어휘 규칙을 끄기만 했는데, **사전형 병음이 들어오면 성조 변화가 아예 적용되지
  않는 문제**가 새 데이터에서 드러났다(一 90건). 한자를 받는 것이 옳은 답이다.
- `check-spec-zh.mjs` 46 → **52 케이스** (음절 분리 6건 신설).
- `verify-pinyin.mjs` 에 `DICT_BLIND` 27종 — pypinyin 이 문맥을 못 잡는 다음자를 좁게
  등록했다(`地` de · `长` cháng · `弹` tán · `杠` gàng · `耳` ěr · `转` zhuàn …).
  이것이 없으면 진짜 오류가 소음에 묻힌다.

### Verified — 784항목 전수

읽기 불일치 **0건** · 성조 불일치 **0건** · 경성 처리 154건(정상) · 로마자 0건 ·
규칙 준수 33/52/37 전부 100% · 데이터 ↔ 생성기 784/784.

## [0.13.0] — 2026-08-25

**음성 폐루프 검증 신설 — 원어민 없이 "이대로 읽으면 알아듣는가" 를 잰다.**

### Added

- `scripts/verify-loop.mjs` — 한글 표기 → 한국어 TTS(macOS `say`) → 원어 ASR(mlx-whisper)
  → 원문 복원 여부. ASR 이 가나를 한자로 바꿔 적는 습관은 **원어 TTS 를 같은 ASR 에
  통과시킨 기준선**으로 상쇄한다. `--ab` 모드로 두 표기를 같은 조건에서 견준다.

  | 언어 | 글자 F1 | 80% 이상 | 판정 |
  | --- | --- | --- | --- |
  | 일본어 76건 | 77.3% | 48/76 | 유효 |
  | 중국어 76건 | 25.7% | 7/76 | 이 방법으로는 판정 불가 (성조가 기호에 있다) |

- **미결이던 J1 을 읽기 실험으로 확인했다.** `떼`(SPEC) 대 `테`(구 데이터):
  단어 4승 1패, 문장 2승 2무 0패. `테` 의 패배는 파국적(`미테네` → `下に`, 0%)이고
  `떼` 의 유일한 패배는 경미하다(`つけって`, 86%). **측정은 SPEC 을 지지하거나 중립이다.**

### 기록 — 새로 드러난 위험

- **어두 탁음이 최대 약점이다.** `ごはん` 고˚항 → `コアン`, `だいすき` → `タイスキー`.
  한국어 어두 평음이 무성음이라 유성 지시가 오직 `˚` 에만 실려 있다. 사람은 기호를 보지만,
  기호를 놓치면 통하지 않는 수준이 된다. J2 의 기호 의존도 재검토 가치 있음(지금은 유지).
- **ん 받침 뒤 조사 연음** — `홍 오` 를 붙여 읽으면 `ほんご`. 띄어 적어도 막지 못한다.
- 측정 방법으로 설명되지 않는 진짜 후보는 3건뿐이다(`くつを`·`ほんを よもう`·`これは`).

## [0.12.0] — 2026-08-25

**원천 데이터 검증 층 신설.** 그동안의 검사는 전부 "한글이 병음·가나와 맞는가" 만 봤다 —
**병음·로마자 자체가 맞는지는 아무도 보지 않았다.** 병음이 틀리면 생성기는 그 틀린 병음을
충실하게 한글로 옮기고 세 층 전부 통과한다. 원어민 감수가 불가능한 상황에서 이 층이
"AI 검수" 의 첫 단계다 — LLM 판정이 아니라 **사전 대조**라 증거로서 가장 강하다.

### Added

- `scripts/verify-pinyin.mjs` — 한자 → pypinyin 사전 병음 → 우리 성조 변화 → 데이터 병음
  대조. 결과: **읽기 불일치 0건**(다음자 오선택 없음), 성조 불일치 0건(교정 후),
  경성 처리 14건(오류 아님), 사전 한계 1건(`哇` — 문두 감탄사 wā 를 pypinyin 은 문말
  조사 wa 로 준다. 데이터가 맞다).
- `scripts/verify-romaji.mjs` — 가나 → 로마자 기계 생성 후 데이터 로마자 필드와 대조.
  일본어는 원문이 가나라 읽기 모호성이 없어 사전이 필요 없다. 결과 **불일치 0건**.
  데이터의 로마자는 가나 충실형(워프로식)이라 `よう` you · `ぎゅー` gyuu · `こっち`
  kocchi 로 적힌다 — 생성 규칙을 그 방식에 맞췄다.

### Fixed

- **어휘 성조 변화(C8·C9)가 동음자에 오발화하던 것** — `衣 yī`(衣服)가 一 규칙에 걸려
  yì 로 뒤집혔다. pypinyin 대조가 잡아냈다. C7(3성+3성)은 **음운 규칙**이라 성조만 보고
  판정하므로 안전하지만, C8(不)·C9(一)은 **어휘 규칙**이라 병음만으로는 `布·步·部 bù`
  같은 동음자를 가릴 수 없다. 이제 **기본으로 끄고**(1단계에서는 입력이 이미 변화형이라
  필요도 없다) 한자를 아는 호출자만 `{lexical:true, chars}` 로 켠다.
- `check-spec-zh.mjs` 43 → **44 케이스** — C8·C9 는 `--lexical` 로 검증하고,
  `Yīfu.` 케이스를 새로 넣어 **어휘 규칙이 오발화하지 않는 것**을 회귀로 고정했다.

## [0.11.0] — 2026-08-25

**SPEC.md 를 v1.3 으로 고쳐 J1 의 빈틈을 막았다.** 앞 버전에서 "데이터를 고치고 SPEC 은
그대로 둔다" 고 했는데, 그 판단의 전제였던 "저자의 표기 관행" 이 **존재하지 않았다** —
이 앱의 중국어·일본어 콘텐츠에는 원저자가 없고 전량 이전 세대 생성물이다(SPEC 이 참조하는
종이 교재는 영어뿐). 즉 `て`→테 는 사람의 음성 직관이 아니라 **규칙이 덜 적용된 흔적**이다.

측정이 그것을 뒷받침한다. 원본 표기는 두 규범 어느 쪽과도 일치하지 않는 **혼입**이었다 —
`と`·`た` 는 SPEC 쪽(또·따), `て` 와 `ました` 의 `た` 는 외래어 표기법 쪽(테·타). 빈틈은
J1 이 た행 예시를 하나도 주지 않은 것이었으므로, **데이터가 아니라 규칙을 조인다.**

### Changed — SPEC v1.3 (웹사이트 저장소 `demo-apps/hangul-pron/SPEC.md`)

- **J1 명료화** — "어두" 를 **발음 낱말의 첫머리**로 정의하고 た행 예시를 넣었다
  (`たべて` 타베떼 · `みつけた` 미츠께따 · `ひとくち` 히또꾸치). `ち`·`つ` 는 위치 무관.
- **J1a 신설** — 촉음 뒤 예외의 경계는 "촉음이냐" 가 아니라 **앞 받침이 무엇이냐**다.
  ㄱ·ㅂ받침 뒤는 된소리(육꾸리·입빠이), ㅅ받침 뒤는 어두형(촛토·맛테).
- **J1b 신설** — ぱ행도 어중에서 된소리. 전에는 예시로만 암시됐다.
- **J8 신설** — ㅈ·ㅊ 뒤 반모음 생략(조·초·차). 생성기 정책이던 것을 규칙으로 올렸다.
- **J6 보강** — 조사 분리 기준과 **"분리가 소리를 바꾸면 안 된다"**.
- 외래어 표기법과 반대인 이유를 각주로 명시하되, **J1 은 검증된 음성학이 아니라 설계
  결정**임을 함께 적었다.

### Changed — 스킬

- `check-spec-ja.mjs` 31 → **37 케이스** (J1a·J1b·J8 분리·신설). 세 언어 33·43·37 전부 100%.
- SKILL.md 의 "SPEC 밖 — 생성기 정책" 표에서 ㅈ·ㅊ·조사 분리를 뺐다(이제 SPEC 에 있다).
  남은 것은 종조사 `ね`, `だ` 분리, 문장부호 뒤 공백, 장음 범위(`えい`) 넷이다.
- SPEC-HANGUL-PRON-002 의 "미결 — J1 의 た행" 을 **"결정"** 으로 닫았다.

## [0.10.1] — 2026-08-25

제2 의견(`gpt-5.6-sol`)에 적대적 검토를 요청해 나온 수정이다. **결론(SPEC 유지·데이터 정규화)은
유지되지만, 근거의 강도를 낮추고 실제 버그 두 개를 고쳤다.**

### Fixed

- **조사 분리가 소리를 바꾸던 것** — 뗀 조각의 첫 모라는 어두가 되므로 `から` 를 떼면
  `さむいから` 가 사무이**까**라에서 사무이 **카**라로 뒤집혔다. か·た행으로 시작하는 조사는
  떼지 않는다. 띄어쓰기는 호흡의 표기일 뿐 소리를 바꾸지 않는다(원리 5·6).
  (`から` 를 분리 목록에 넣은 근거가 데이터 1건이었다 — 함정 2를 내가 어긴 자리다.)
- **띄어쓰기 유무로 결과가 갈리던 것** — `ごはんをたべよう` 가 고항오**따**베요ー 였다.
  `を` 는 언제나 조사이므로 **낱말 안에 있어도 끊는다.** 이제 `ごはんを たべよう` 와 같다.
- **`はを`(歯を, 이를)의 は 를 조사로 읽던 것** — 하 가 와 로 뒤집혔다. 조사인지 여부를
  위치로 짐작하지 않고 값으로 들고 다닌다.

### Added

- `check-data.mjs`(웹사이트 저장소)에 **J1 검사** — ㅋ·ㄲ 는 か행에서만, ㅌ·ㄸ 는 た·て·と
  에서만 나오므로 개수가 정확히 일치해야 한다. **이 검사가 없어서 "위반 0건" 이 정작 문제의
  규칙을 검증하지 못하고 있었다.** 붙이자마자 위 `から` 버그가 잡혔다.
- `check-spec-ja.mjs` 케이스 3건 추가 (28 → 31): `さむいから` · `はを` · `ごはんをたべよう`.
- SKILL.md 에 **"SPEC 밖 — 생성기 정책"** 절. ㅈ·ㅊ 뒤 반모음 생략, 조사·`だ` 분리 기준,
  종조사 `ね`, 문장부호 뒤 공백은 SPEC 에 없는 **생성기 정책**이다 — 데이터와 어긋나면
  SPEC 위반이 아니라 정책 차이로 다룬다. "생성기 = SPEC 산출물" 이라는 말이 이 부분에서
  정확하지 않았다.

### Changed — 근거의 강도를 낮춤

- SPEC-HANGUL-PRON-002 에 **"미결 — J1 의 た행"** 절 신설. `て` 를 떼로 적는 것은
  **SPEC 문면을 따른 정규화**이지 음성학적 정답 판정이 아니다. 확정되지 않은 것을 명시했다:
  비어두 `て` 10건이 전부 `-て`형이라 "글자 예외" 와 "문법형 관행" 을 구별할 수 없고,
  반례로 든 `できました` 는 모음 무성화 환경이라 깨끗한 최소대립이 아니며, 한국어 청자에게
  일본어 **어중** 무성 파열음은 경음·격음 양쪽으로 갈린다는 실험이 있다.
- J1 문면 후보 두 가지(명료화만 / 관용 명문화)와 **읽기 실험 설계**를 함께 기록했다.
  판단에 필요한 것은 더 많은 문자열이 아니라 사람이 읽은 소리다.

## [0.10.0] — 2026-08-24

### Added

- `hangul-pron` v0.5.0 — **일본어 1단계(가나 입력).** `node scripts/ja.mjs "ごはんを たべよう。"`
  → `{고=v}항 오 타{베=v}요ー.` 한자 혼용문이 아니라 가나를 받는다 — 요미를 얻으려면 형태소
  분석이 필요하므로 2단계로 미뤘다(SPEC-HANGUL-PRON-002 설계). 이로써 세 언어 모두 1단계 완료.
- `scripts/kana.mjs` — 모라 분리 · 장음 판정 · 조사 떼어내기. 모라는 글자와 일대일이 아니라
  (요음·촉음·발음·장음) 여기서 정리해 넘긴다. 읽지 못한 글자는 `rest` 로 보고한다.
- `scripts/ja.mjs` — 모라 → 한글 + 기호 (J1~J7).
- `scripts/check-spec-ja.mjs` — 일본어 정본 지표. J1~J7 28케이스, 현재 100%.
  **SPEC §2-3 예시를 전량 포함**한다(かわいい·いく·はこう·ごはん·だめ·さんぽ·おいしい·
  くつ·がっこう·いっぱい·ちょっと).
- `scripts/eval-ja.mjs` — 참고 지표. 앱 데이터 76건과 문자열 대조 (문장 76.3% · 글자 96.9%).

### Changed

- `docs/specs/SPEC-HANGUL-PRON-002.md` — 상태를 "1단계 완료" 로. 일본어 결과와 **구현하며
  걸린 7가지**를 추가했다(모라≠글자, 장음은 ー 만이 아님, 촉음 뒤는 3상태, ぱ행 어중 된소리,
  조사 분리의 2모라 기준, だ 는 だよ·だね 로만, ㅈ·ㅊ 뒤 반모음 생략).

### Verified

- 세 언어 정본 지표 33/33 · 43/43 · 28/28 (전부 100%).
- 중국어·일본어 생성 출력을 3언어 앱 `check-data.mjs` 에 넣어 검사 — **위반 0건** (152항목).

### 미결 — SPEC 판단이 필요하다

- `SPEC.md` J1 이 "어두 か·た행 → ㅋ·ㅌ, 어중 → ㄲ·ㄸ" 라고만 하고 **た행 예시를 주지
  않는다**(예시 셋이 전부 か행). 앱 데이터는 た·と 는 따·또로 적으면서 어말 て 만 테로
  적는다(9:1). 구현은 SPEC 문면을 따라 떼 를 내며, 이 갈림이 참고 지표 불일치 18건 중
  9건이다. 교재·앱 전체에 걸리는 결정이라 스킬 쪽에서 바꾸지 않았다.

## [0.9.0] — 2026-08-24

### Added

- `hangul-pron` v0.4.0 — **중국어 1단계(병음 입력).** `node scripts/zh.mjs "Wǒmen chī fàn ba."`
  → `{워=3}먼 {츠=1#} {판=4o} 빠.` 한자가 아니라 병음을 받는다 — 한자→병음은 사전과
  다음자(多音字) 문제가 따라오므로 2단계로 미뤘다(SPEC-HANGUL-PRON-002 설계).
- `scripts/pinyin.mjs` — 병음 음절 분리(성모+운모 최장일치) · 성조 변화 C7~C10 ·
  변화 후 병음 재작성. 3언어 앱 `check-data.mjs` 의 `splitPinyin()` 을 옮기며 **소비하지 못한
  글자를 `rest` 로 보고**하도록 고쳤다 — 원본은 조용히 버려서 얼화의 남는 `r` 이나 오타가
  사라진 채 검증을 통과했다.
- `scripts/zh.mjs` — 성모·운모 → 한글 + 기호 (C1~C6a·C10). 성조 변화는 기본 적용이고
  **사전형에만 발화**해 멱등이다 — 이미 변화 후인 병음(앱 데이터가 그렇다)을 넣어도 안전하다.
- `scripts/check-spec-zh.mjs` — 중국어 정본 지표. C1~C10·기호·덩어리 성조·병음 줄 43케이스,
  현재 100%. 영어 33케이스와 파일을 나눴다 — 섞으면 한 언어의 회귀가 다른 언어 숫자에 묻힌다.
- `scripts/eval-zh.mjs` — 참고 지표. 3언어 앱 데이터 76건과 문자열 대조
  (문장 96.1% · 덩어리 99.7%). 남은 3건은 저자 재량이라 규칙으로 옮기지 않았다.

### Changed

- `docs/specs/SPEC-HANGUL-PRON-002.md` — 상태를 In progress 로, 중국어 1단계 결과와
  **구현하며 새로 걸린 6가지**를 추가했다(대소문자 우선 처리, y·w 는 성모가 아님,
  활음 합치기 조건, sh+우 는 슈, 성조 변화의 멱등성, 경성화된 4성).

### Verified

- 생성 출력을 3언어 앱 `check-data.mjs` 에 넣어 검사 — **위반 0건** (152항목).
- 영어 `check-spec.mjs` 33/33 유지 (회귀 없음).

## [0.8.0] — 2026-08-24

### Added

- `scripts/holdout.mjs` — 훈련/평가 분할 검증 하네스. 임시 폴더와 상대경로 이웃(g2p.mjs·
  cmudict.dict)을 스스로 준비하고 끝나면 지운다.
- `docs/specs/SPEC-HANGUL-PRON-002.md` (모노레포 docs) — **중국어·일본어 확장 지침.**
  측정으로 확인된 함정 8가지를 담았다: 한글에서 출발하면 안 되는 이유(음절 42% 모호),
  교재 재현이 목표가 아닌 이유(교재 자기 불일치 17%), 사전 확장이 새 문장에 안 듣는 이유
  (hapax 51%), 홀드아웃 없이 재면 안 되는 이유(+17.9pp 중 대부분이 암기), 기호 재현율
  분모의 겹자 접기, 병음 공백 분리 금지(오탐 49건 중 44건의 원인), 경성도 C1 대상,
  한 글자 기호 2개 허용(전사본은 예외).
- SKILL.md 에 그 지침으로 가는 링크.

## [0.7.0] — 2026-08-24

### Changed

- `hangul-pron` v0.3.0 — **정본 지표를 교재 일치도에서 SPEC 규칙 준수로 바꿨다.**
  따라야 할 것은 교재의 문자열이 아니라 표기 **철학**(E1~E8 + 기호 3종)이라는 판단이다.
  교재는 같은 문장을 다르게 적고(중복 46종 중 17%) 저자 재량 축약(`last`→라쓸)도 있어,
  문자열 일치를 목표로 삼으면 규칙을 왜곡하게 된다. 교재 일치도는 회귀 감시용 참고 지표로 강등.

### Added

- `scripts/check-spec.mjs` — SPEC 규칙 준수 검사기(33케이스, 현재 100%). 문자열 일치가 아니라
  규칙의 결과가 나타나는지를 **자모 단위**로 본다(`스삩` 처럼 된소리가 받침과 합쳐진 글자를
  음절 리터럴로 잡으려다 검사가 먼저 틀리는 일이 실제로 있었다).

### Fixed

- E4 위반 — `all` 이 `올` 로 나오던 것을 `오을` 로 교정(어말 l → ㄹ+을). 교재는 두 표기를
  오가지만 SPEC 이 정본이다.

## [0.6.0] — 2026-08-24

### Changed

- `hangul-pron` v0.2.0 — 미등록어 처리를 **철자 규칙에서 G2P(CMUdict) 로 교체**.
  홀드아웃 평가(훈련 80%/평가 20%, 평가 셋 157문장) 기준 한글 표기 정확도
  **34.4% → 51.0%**, 음절 유사도 74.6% → 88.2%. 기호 재현율은 R·F·V·Th 모두 100% 유지.
- 계기는 측정이었다. 코퍼스 어휘의 **51%가 1회성**이라 사전을 키워도 새 문장에는 +3.2pp 뿐인데,
  처음 보는 단어를 근사하는 G2P 는 +16.6pp 를 준다. 사전 자동 채굴이 in-domain 에서 +17.9pp 를
  낸 것은 대부분 **암기**였고, 홀드아웃 분할로만 드러났다.
- 기호 재현율 지표의 분모 결함 교정 — 겹자(`off` 의 ff, `carrot` 의 rr)를 두 소리로 세어
  재현율이 실제보다 낮게(93.8%) 나오고 있었다. 접어서 세면 100% 다.

### Added

- `data/cmudict.dict` (3.6MB, BSD-2-Clause) · `scripts/g2p.mjs` — 음소→한글 합성기,
  철자→음소 규칙, CMUdict 로더. 사전이 없어도 철자 규칙으로 폴백한다.
- 단어 경계 연음 — `put it in` → `푸리린`. 앞말에 붙는 기능어에만 적용한다
  (무차별 적용은 3.2pp 하락을 실측).
- ARPAbet→한글 매핑 규칙 3건 — 홀로 온셋인 R 뒤 OW 원순화(`roll`→뤄ˇ을), CH 뒤 전설모음
  활음화(`cheese`→취즈), 파열음 받침 + L 온셋에 `클·블·플` 삽입(`quickly`→퀵클리, L 유실 방지).
  단어 단위 dev 84% → 87%. R 규칙은 처음에 자음군까지 적용해 `stroke`→스뜨뤅 으로 망가뜨렸고,
  온셋 단독일 때로 좁혀 교정했다.

## [0.5.0] — 2026-08-24

### Added

- `hangul-pron` (v0.1.0) — 영어 문장을 한글로 소리 나는 대로 적는 규칙 기반 변환기.
  한국어 화자가 약한 **R·F/V·Th** 에 기호(ˇ ˚ ˜)를 얹는 것이 목적이며, 한글 표기보다
  기호를 빠뜨리지 않는 것을 우선한다. 교재 784문장 기준 기호 재현율 R 99.8% · F·V 93.8% ·
  Th 100% 로 **사람이 적은 교재(88.3 / 90.1 / 93.4%)를 상회**한다 — 규칙 기반의 이점이
  나오는 지점이다. 평가 코퍼스는 시중 교재 전사본이라 저장소에 두지 않고 `eval.mjs` 가
  경로를 인자로 받는다.
- `link-skills.sh` — 이 플러그인의 스킬을 `~/.claude/skills/` 에 심볼릭 링크로 거는 스크립트.
  멱등이며, 실제 디렉토리는 절대 지우지 않는다(다른 곳을 가리키는 링크만 `--force` 로 교체).
  `--dry-run` · `--unlink` 지원.

### Notes

- `hangul-pron` 은 하이브 데스크톱 스택에 의존하지 않는다. 이 플러그인의 편입 기준
  ("스택 없이 코어가 동작하는가")과는 맞지 않으므로, 안정화 후 `itda-work` 등으로
  옮기는 것을 검토할 것.

## [0.4.0] — 2026-08-11

### Added

- **`orca-coach` 스킬 신설** — Orca 기능 활용 코치. 정적 FAQ가 아니라 **상황→기능 추천 엔진**:
  추천 전에 `orca` CLI·git 으로 현재 세션 상태를 진단하고, playbook 의 상황 매핑 표
  (6개 상황군 29행 + Claude Code × Orca 조합 레시피 8종)에서 2~3개만 골라 추천한다.
  기능 설명 본문은 복제하지 않고 개인 스킬 `orca-guide` 의 `docs/` 경로 포인터만 갖는다
  (orca-guide 미설치 환경에서는 포인터 대신 공식 문서 URL 로 안내). 상세 사용법은
  orca-guide, 실행은 orca-cli/orchestration, 신기능 확인은 changelog 로 핸드오프.
  ②축(개발 데스크톱 도구 — Orca IDE 표면)의 두 번째 사례.

## [0.3.0] — 2026-08-10

### Added

- **`changelog` 스킬 신설 (#1476)** — Orca(onorca.dev)·Claude Code·Codex CLI 의 최근 릴리즈를
  수집(gh api)→한국어 요약→HTML 렌더→Orca 내장 브라우저 탭으로 잇는 파이프라인. 제품 프로파일
  (`profiles/*.json`) 기반 파서 3종: conventional(orca `* feat(scope):`)·prose(claude-code
  `- Added/Fixed …` 동사→종류, `[VSCode]` 브래킷→표면)·sections(codex 큐레이션 섹션→종류,
  복수 PR 참조, 말미 PR 전량 덤프 분리 보존). 동작 변경·되돌림 최상단 표면화 + 원문 전량 보존.
  실행 상태(last_seen·탭 id)는 `~/.local/state/itda-changelog/` (XDG, 머신 로컬).
  개인 스킬 `orca-changelog` 의 개명·일반화 승격.

### Changed

- **편입 기준 개정 (#1476, 마스터 결정 2026-08-10)** — "hyve 없이 코어가 동작하는가" →
  **"하이브 데스크톱 스택(hyve MCP + 개발 데스크톱 도구) 없이 주경로가 성립하는가"**.
  hyve MCP 를 안 쓰지만 Orca IDE 가 산출 표면인 `changelog` 가 ②축(개발 데스크톱 도구)의
  첫 사례. 코어 자립 스킬은 도메인 플러그인이라는 #1301 의 정신은 불변.

## [0.2.0] — 2026-07-27

### Removed

- **`web-reader` → `itda-work` 복귀 (#1301)** — #1299 의 편입은 분류 오류였다. 코어가 자체 HTTP
  페치(`curl_cffi`)로 자립하고 hyve `web_browse` 는 **차단 사이트 에스컬레이션 폴백**일 뿐인데,
  본문의 `web_browse` 언급 횟수(15회)를 정체성 신호로 과대평가해 hyve 전용으로 판정했다.
  플러그인은 100% hyve 의존 2종(`web-automation`·`pptx-diff`) 체제로 존치한다.
- 편입 기준을 README 에 명문화 — **"hyve 없이 코어가 동작하는가"**. 폴백으로만 hyve 를 쓰는
  스킬은 도메인 플러그인에 남긴다.

## [0.1.0] — 2026-07-27

### Added

- **itda-hyve 플러그인 신설 (#1299)** — hyve 데스크톱 앱의 MCP 도구를 소비하는 스킬의 전용 홈.
  "hyve 가동 + 프리셋 등록"이라는 공통 전제를 플러그인 경계로 끌어올려, 설치 단위와 요구사항을 일치시킨다.
- **`pptx-diff` 1호 스킬** — hyve `office_read` MCP의 `diff` 액션으로 PPTX 두 버전(git 리비전 `from`/`to`
  또는 별도 파일 `against`)을 비교하고 슬라이드·도형·텍스트 변경을 한국어로 요약한다. 순수 프롬프트 스킬(스크립트 0).

### Changed

- **`web-automation`·`web-reader` 이전 (#1299)** — `itda-work` 에서 이관. 스킬 `name` 은 불변이며
  플러그인 접두사만 `itda-work:` → `itda-hyve:` 로 바뀐다. `blog-reader`(itda-work)가 `web-reader` 의
  `fetch_html.py` 를 subprocess 로 위임하는 크로스-스킬 의존은 형제 플러그인 경로 탐색을 추가해 유지된다.
