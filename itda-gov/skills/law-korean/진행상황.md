# law-korean 진행상황

> 마지막 갱신: 2026-03-30

## 현재 위치

- 기준 맥락: `HANDOFF.md` + SPEC-LAW-003/004 완료 상태에서 Phase 3 착수
- 이번 작업 범위: **신구법 상세 조회 + 조문 비교 + 법령용어 검색/관계/양방향 연계 조회까지 진행**
- 작업 원칙: 작은 단위로 구현 → 테스트 → 실제 API 검증 → 다음 단계 진행

## 이번에 완료한 항목

- `scripts/old_and_new_api.py` 추가
  - `target=oldAndNew` 목록 검색 구현
  - 1시간 TTL 캐시 + `no_cache` 지원
  - 신구법 상세 조회 구현 (`ID` 또는 `MST`)
  - 24시간 TTL 캐시 + 조문 텍스트 정리
- `scripts/search_old_and_new.py` 추가
  - 옵션: `--query`, `--display`, `--page`, `--format`, `--oc`, `--no-cache`
  - 출력: `table`, `json`, `md`
- `scripts/get_old_and_new.py` 추가
  - 옵션: `--id` 또는 `--mst`, `--format`, `--oc`, `--no-cache`
  - 출력: 구법/신법 메타데이터 + 구조문/신조문
- `scripts/compare_articles.py` 추가
  - 옵션: `--left-id|--left-name`, `--right-id|--right-name`
  - `--left-article`, `--right-article` 기반 조문 비교
  - 출력: `text`, `json`, `md`
  - 2차 슬라이스에서 구조화 결과 payload + Markdown diff 출력 추가
  - 후속 슬라이스에서 조문 제목 + diff 요약(추가/삭제 줄 수) 출력 추가
  - 후속 슬라이스에서 `--summary-only` 추가
  - 후속 슬라이스에서 `--max-diff-lines` 추가 (기본 80)
- `scripts/lstrm_api.py` 추가
  - `target=lstrmAI` 법령용어 검색 구현
  - 1시간 TTL 캐시 + `homonymYn` 필터 지원
- `scripts/search_lstrm.py` 추가
  - 옵션: `--query`, `--display`, `--page`, `--homonym-yn`, `--format`, `--oc`, `--no-cache`
  - 출력: `table`, `json`, `md`
- `scripts/get_lstrm_rlt.py` 추가
  - 옵션: `--mst`, `--format`, `--oc`, `--no-cache`
  - 출력: `text`, `json`, `md`
  - `target=lstrmRlt` 기준 일상용어 관계 상세 조회
- `scripts/get_lstrm_rlt_jo.py` 추가
  - 옵션: `--mst`, `--format`, `--oc`, `--no-cache`
  - 출력: `text`, `json`, `md`
  - `target=lstrmRltJo` 기준 법령용어-조문 연계 상세 조회
  - 후속 슬라이스에서 `--summary-only` + 조문 본문 30,000자 트런케이션 추가
  - 후속 슬라이스에서 `--max-items` 추가 (기본 20)
  - 후속 슬라이스에서 중복 조문 자동 제거 추가
- `scripts/get_jo_rlt_lstrm.py` 추가
  - 옵션: `--id`, `--jo`, `--format`, `--oc`, `--no-cache`
  - 출력: `text`, `json`, `md`
  - `target=joRltLstrm` 기준 조문-법령용어 역방향 연계 조회
  - 후속 슬라이스에서 `--summary-only` + 조문 본문 30,000자 트런케이션 추가
  - 후속 슬라이스에서 `--max-items` 추가 (기본 20)
  - 후속 슬라이스에서 중복 법령용어 자동 제거 추가
- `scripts/law_formatter.py` 확장
  - `format_old_and_new_search_md()` 추가
  - `format_old_and_new_detail_md()` 추가
  - `format_lstrm_search_md()` 추가
  - `format_lstrm_rlt_md()` 추가
  - `format_lstrm_rlt_jo_md()` 추가
  - `format_jo_rlt_lstrm_md()` 추가
- 테스트 추가
  - `scripts/tests/test_old_and_new_api.py`
  - `scripts/tests/test_search_old_and_new.py`
  - `scripts/tests/test_get_old_and_new_api.py`
  - `scripts/tests/test_get_old_and_new.py`
  - `scripts/tests/test_compare_articles.py`
  - `scripts/tests/test_lstrm_api.py`
  - `scripts/tests/test_search_lstrm.py`
  - `scripts/tests/test_get_lstrm_rlt_api.py`
  - `scripts/tests/test_get_lstrm_rlt.py`
  - `scripts/tests/test_get_lstrm_rlt_jo_api.py`
  - `scripts/tests/test_get_lstrm_rlt_jo.py`
  - `scripts/tests/test_get_jo_rlt_lstrm_api.py`
  - `scripts/tests/test_get_jo_rlt_lstrm.py`

## 검증 결과

- 신규 테스트: **153개 통과**
- law-korean 전체 테스트: **454개 통과**
- 수정 파일 LSP diagnostics: **0 error**
- 실제 API 검증:
  - `search_law.py --query "근로기준법"` 정상
  - `get_law.py --name "근로기준법" --article 60` 정상
  - `search_old_and_new.py --query "근로기준법" --display 1 --no-cache` 정상
  - `search_old_and_new.py --query "근로기준법" --display 1 --format md --no-cache` 정상
  - `get_old_and_new_detail(mst="265959")` 정상 (`구법 32조문 / 신법 32조문`)
  - `get_old_and_new.py --mst 265959 --format md --no-cache` 정상
  - `compare_articles.py --left-name "근로기준법" --left-article 60 --right-name "남녀고용평등과 일ㆍ가정 양립 지원에 관한 법률" --right-article 19 --no-cache` 정상
  - `compare_articles.py ... --format json` / `--format md` 테스트 통과
  - `compare_articles.py --left-name "근로기준법" --left-article 60 --right-name "근로기준법" --right-article 61 --format md --no-cache` 정상
  - `compare_articles.py` 출력에 조문 제목 + diff 요약 반영 확인
  - `compare_articles.py --summary-only --format md` 정상
  - `compare_articles.py --max-diff-lines 20 --format md` 정상
  - 잘못된 법령명 입력 시 `오류: 좌측 법령을 찾을 수 없습니다: '없는법'` 확인
  - `search_lstrm.py --query "통상임금" --no-cache` 정상
  - `search_lstrm.py --query "통상임금" --format md --no-cache` 정상
  - `get_lstrm_rlt.py --mst 1280461 --no-cache` 정상
  - `get_lstrm_rlt.py --mst 1280461 --format md --no-cache` 정상
  - `get_lstrm_rlt.py` live 관계 목록 dedupe 확인
  - `get_lstrm_rlt_jo.py --mst 1280461 --no-cache` 정상
  - `get_lstrm_rlt_jo.py --mst 1280461 --format md --no-cache` 정상
  - `get_jo_rlt_lstrm.py --id 000130 --jo 001800 --no-cache` 정상
  - `get_jo_rlt_lstrm.py --id 000130 --jo 001800 --format md --no-cache` 정상
  - `get_lstrm_rlt_jo.py --mst 1280461 --summary-only --format md --no-cache` 정상
  - `get_jo_rlt_lstrm.py --id 000130 --jo 001800 --summary-only --format md --no-cache` 정상
  - `get_lstrm_rlt_jo.py --mst 1280461 --summary-only --max-items 5 --format md --no-cache` 정상
  - `get_jo_rlt_lstrm.py --id 000130 --jo 001800 --summary-only --max-items 5 --format md --no-cache` 정상
  - live 출력에서 중복되던 `근로기준법 제43의8조`, `고용보험법 시행령 제95의3조`, `출산전후휴가` 항목 제거 확인

## 다음 작업 후보

1. 조문 비교 diff 노이즈 추가 완화(예: 공통 접두부/머리말 요약) 검토
2. compare diff 추가 요약(공통 접두부/머리말 압축) 검토
3. 문서/스킬 사용법 반영

## 메모

- 진행상황은 이 문서에 계속 누적한다.
- 기능은 항상 **테스트 가능한 최소 단위**로 끊어서 추가한다.
