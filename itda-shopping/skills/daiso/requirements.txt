# 표준 라이브러리만 사용 — HTTP 페치는 urllib 자체 수행, 외부 의존성 없음.
#
# 단, 매장별 재고(inventory)·진열 위치(display-location) 2기능의 AES 경량 인증에
# 한해 `cryptography` 가 필요할 수 있음. 미설치 시 해당 2기능만 graceful degrade
# 하며, 나머지(products·price·stores)는 stdlib만으로 정상 동작함.
#
# 설치(해당 기능을 쓸 때만):
#   uv pip install --system cryptography
