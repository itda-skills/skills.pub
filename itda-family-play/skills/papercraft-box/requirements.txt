# papercraft-box 의존성
reportlab>=4.0        # PDF 생성(필수). TrueType(glyf) 폰트만 지원 — CFF 폰트는 fontpick 이 건너뛴다.
PyMuPDF>=1.23.0       # verify(기하 검증)·--preview(PNG) 용. 없으면 build 만 되고 verify 는 생략된다.
Pillow>=9.0          # render(조감도 PNG) 용. reportlab 이 이미 끌어오지만 render3d 가 직접 import 하므로 명시.
